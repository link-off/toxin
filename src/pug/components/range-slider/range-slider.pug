mixin range-slider({className, isDescription=false, name, value, description}={})
  -
    var componentClassName = 'range-slider'
    componentClassName = className ? ' ' + className : ''
    componentClassName += isDescription ? ' ' + componentClassName + '_desc': ''
  div(class= componentClassName)
    .range-slider__wrapper
      .range-slider__name= name
      .range-slider__value= value
    input.range-slider__input(type='range' min='0' max='100' value='5')
    input.range-slider__input(type='range' min='0' max='100' value='75')
    .range-slider__slider
      .range-slider__track
      .range-slider__range
      .range-slider__thumb.range-slider__thumb_left
      .range-slider__thumb.range-slider__thumb_right
    if description
      .range-slider__description= description  